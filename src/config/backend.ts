/** Adresse backend par défaut intégrée dans l'application.
 *  Modifiez cette valeur avant de publier si le Raspberry Pi utilise une autre adresse.
 *  Elle est utilisée automatiquement sauf si l'utilisateur la remplace dans les paramètres.
 */
export const DEFAULT_API_BASE = "http://10.42.0.1:5000";

/** URL publique du frontend (page du photobooth). */
export const FRONTEND_URL = "https://photoboothmati.lovable.app";

